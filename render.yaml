services:
  - type: web
    name: nfl-pick-em
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: PORT
        value: 10000
